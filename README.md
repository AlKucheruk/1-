# Проект восстановления данных в 1С УТ

## О чем проект

Проект восстанавливает корректный контур взаиморасчетов и движения денег в `1С:Управление торговлей` на основе исторически корректных данных из `1С:Бухгалтерия предприятия`.

Основная идея:

* `1С БП` используется как источник истины по платежам;
* `1С УТ` приводится в состояние, где корректно отражены контрагенты, договоры и банковские документы;
* после загрузки данные в УТ позволяют достоверно анализировать ДЗ/КЗ и денежные потоки.

## Начальная точка

На старте в УТ были проблемы с качеством данных:

* часть контрагентов и реквизитов отсутствовала или была заполнена неполно;
* в документах реализации не везде были корректно проставлены соглашения и договоры;
* карточки ИП/ФЛ требовали нормализации;
* банковские платежи в УТ были неполными или некорректными относительно БП.

## Конечная цель

Получить в УТ рабочий и проверяемый слой данных:

* контрагенты приведены к корректному виду;
* договорная аналитика согласована для корректного учета;
* платежки из БП загружены в УТ без дублей (по УИД);
* оставшиеся сложные случаи выделены в ручной контроль.

## Пошаговый маршрут

### Этап 1. Подготовка клиентов для внесения в УТ

* Выгружаются списки клиентов из БП и УТ.
* Python-скрипт сравнивает данные, находит отсутствующих в УТ клиентов и дополняет банковские реквизиты.
* На выходе: подготовленный файл для последующей загрузки/дозаполнения.

### Этап 2. Нормализация ИП и ФЛ

* Python-скрипт обогащает данные по ИНН через сервис ФНС (ЕГРЮЛ/ЕГРИП).
* Внешняя обработка 1С обновляет карточки контрагентов/партнеров в УТ по коду.
* Результат: единый, валидный формат карточек.

### Этап 3. Создание договоров

* Подготавливаются данные для массового заведения договоров.
* Этап закрывает разрывы, мешающие корректной загрузке платежей и аналитике расчетов.

### Этап 4. Дозаполнение цепочек реализации

* Внешняя обработка 1С массово проставляет `Соглашение` и `Договор` в документах реализации и связанных заказах.
* Применение идет с предпросмотром, выбором строк и безопасными транзакциями.

### Этап 5. Дозаполнение приходов договором

* Внешняя обработка по приходным документам поставщика (на базе логики этапа 4): договор, батчи, перепроведение, контроль суммы.

### Этап 6. Порядок расчетов в реализациях

* Копия папки этапа 4: **та же форма** обработки в 1С, другой модуль формы — только `ПорядокРасчетов = ПоДоговорамКонтрагентов` (поле «Расчёты» в правилах оплаты).
* Рабочий BSL: `Этап 6 - Исправление Порядока расчетов реализаций/Код1С_УТ_06_ПорядокРасчетов_Реализации_модульФормы.bsl`.

### Этап 7. Порядок расчетов в поступлениях

* Копия папки этапа 5: **та же форма**, модуль — только смена `ПорядокРасчетов` по списку типов документов, как в этапе 5.
* Рабочий BSL: `Этап 7 - Порядок расчетов поступлений/Код1С_УТ_07_ПорядокРасчетов_Поступления_модульФормы.bsl`.

### Этап 8. Загрузка платежек в УТ

* Python-пайплайн очищает выгрузку банка из БП, применяет маппинг операций и статей ДДС, формирует готовые файлы.
* Внешняя обработка 1С загружает документы `Поступление/Списание`, проверяет дубли по УИД и фиксирует связи в регистре.
* По итогам обеспечивается перенос истории платежей в УТ.

### Этап 9. Выравнивание складских остатков в УТ

* Скрипты и материалы для выявления и выравнивания проблем по складским остаткам (ранее нумеровалось как этап 7).

### Этап 10. Довнесение поступлений в УТ

* Внешняя обработка по Excel: довнесение **поступлений** (см. `Этап 10 - Довносим документы в УТ/README.md`).
* Запросы для сверки журналов УТ/БП и отдельные выборки по БП — в той же папке, имена начинаются с `Запрос_УТ_...` или `Запрос_БП_...`.

### Этап 11. Довнесение реализации в УТ

* Цепочка: **Заказ клиента → Реализация → Счёт-фактура выданный** по Excel (см. `Этап 11 - Довносим реализацию в УТ/README_Этап11_форма_и_колонки.md`).
* Скомпилированная обработка: `Код1СУТ_11_Заполнение_недостающих_Реализаций.epf`.

### Ручной контроль (вне репозитория)

* Исключения, которые нельзя надёжно обработать скриптами, доводятся вручную в базе. Отдельная папка «Этап 99» в репозитории не ведётся.

## Внешние обработки (`.epf`) по этапам

| Этап | Файл `.epf` (в папке этапа) |
|------|----------------------------|
| 1 | `KAA_УТ_Этап_1_Пакетная_Загрузка_Клиентов.epf`, `Код1СУТ_01_Пакетная_Загрузка_Клиентов.epf` |
| 3 | `Код1СУТ_03_СозданиеИДоЗаполнениеДоговоров.epf` |
| 4 | `Код1СУТ_04_Исправление Реализаций.epf` (+ при необходимости `Материалы из src/Добавление_Соглашений_В_Старые_Реализации.epf`) |
| 5 | `Код1СУТ_05_Исправление Поступлений.epf` |
| 6 | `Код1СУТ_06_Исправление Порядока расчетов реализаций.epf` |
| 8 | `Код1СУТ_08_ЗагрузкаПлатежекИзБП.epf` |
| 10 | `Код1СУТ_10_Заполнение_недостающих_Поступлений.epf` |
| 11 | `Код1СУТ_11_Заполнение_недостающих_Реализаций.epf` |

Исходники логики — рядом в виде `.bsl` (модуль формы), кроме случаев когда хранится только `.epf`.

## Ключевые артефакты

* Python: подготовка и нормализация данных (`.py`).
* 1С BSL: массовые обновления и загрузка в УТ (`.bsl`).
* Текстовые запросы/описания: правила выборки и настройки (`.txt`, `.md`, `.docx`); **имена запросов** к УТ/БП в репозитории начинаются с `Запрос_УТ_...` или `Запрос_БП_...` (см. папки этапов).
* Excel-файлы: входные и выходные таблицы для этапов обработки.
* `1c_ut_knowledge.rules` (в **корне** репозитория) — единый файл для агента: предметные факты по УТ 11, типовые ошибки BSL/запросов и краткий **стиль оформления кода** (раздел 15). Раньше стиль был в отдельном `правила.rules` — объединено. Не пользовательская инструкция, а контекст для Cursor.

## Текущее состояние по материалам проекта

Из документов и кода видно, что основная логика пайплайна уже реализована:

* подготовка клиентов и нормализация карточек;
* массовое исправление договорной связности;
* подготовка и загрузка платежек с защитой от повторной загрузки;
* оставшиеся кейсы вне автоматики — вручную в базе (папка «Этап 99» в репо не используется).

Дальнейший фокус - стабильное прогонное тестирование на копии базы и финальный перенос в рабочий контур.

